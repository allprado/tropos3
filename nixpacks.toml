[phases.setup]
aptPkgs = ["wget", "bash", "curl"]

[phases.install]
cmds = [
  "npm ci",
  "chmod +x scripts/install-energyplus-alt.sh",
  "./scripts/install-energyplus-alt.sh"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm start"

[variables]
NODE_VERSION = "20.19.0"
